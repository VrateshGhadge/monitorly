{
  "name": "backend",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@repo/db": "*",
    "@repo/validation": "*",
    "bcryptjs": "^3.0.3",
    "hono": "^4.12.13",
    "resend": "^6.18.1"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
